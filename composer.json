{
    "name": "confish/sdk",
    "description": "Official PHP SDK for confish — typed configuration, actions, logs, feeds, and webhook verification.",
    "type": "library",
    "license": "MIT",
    "homepage": "https://confi.sh",
    "keywords": ["confish", "configuration", "config", "feature-flags", "sdk"],
    "authors": [
        { "name": "confish" }
    ],
    "require": {
        "php": "^8.1",
        "ext-json": "*",
        "guzzlehttp/guzzle": "^7.5",
        "psr/log": "^2.0 || ^3.0"
    },
    "require-dev": {
        "monolog/monolog": "^3.0",
        "phpunit/phpunit": "^10.5",
        "phpstan/phpstan": "^1.11"
    },
    "suggest": {
        "monolog/monolog": "Ship logging you already have to confish in batches via Confish\\MonologHandler (^3.0)"
    },
    "autoload": {
        "psr-4": {
            "Confish\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Confish\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "analyse": "phpstan analyse src --level=8"
    },
    "support": {
        "issues": "https://github.com/confishhq/confish-php/issues",
        "source": "https://github.com/confishhq/confish-php"
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "stable"
}
